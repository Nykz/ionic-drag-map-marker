import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'io.ionic.starter',
  appName: 'drag-map-marker',
  webDir: 'www',
  server: {
    androidScheme: 'https'
  }
};

export default config;
